<div id="mail" class="row">
	<div class="col-md-6 center-block">

		<div class="alert">
			<h4>Do you want to send this E-Mail to <%= details.email %>?</h4>
			<h6><strong>Subject:</strong> New video available</h6>
			<p>Dear <%= details.name %>,</p>
			<p>Since your last login, <%= Video.where(created_at: (details.updated_at)..Time.now).count %> new videos have been uploaded for review.</p>
			<p>You can review them <a href="">here</a> using your password: '<%= details.password %>'.</p>
			<p>Kind regards,<br />
				Aviofilm</p>


			<div class="progress progress-striped active">
				<div class="progress-bar"  role="progressbar" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100" style="width: 10%">
					<span class="sr-only">10% Complete</span>
				</div>
			</div>


			<%= link_to 'Send', mail_admin_client_path(details.id), method: :post, remote: true, class: "btn btn-primary btn-wide", id: "send" %>

			<button id="close" class="btn btn-default btn-wide">
				Cancel
			</button>
		</div>
	</div>
</div>